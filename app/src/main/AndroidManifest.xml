<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.githupuser">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_apk_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_apk_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithupUser">
        <activity
            android:name=".intent.DetailUserActivity"
            android:exported="false" />
        <activity
            android:name=".intent.ListUserActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>